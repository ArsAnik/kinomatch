import React from 'react';
import './button_estimate.css'

const Button_estimate = (props) => {
    return (
        <button className="btn_estimate">Оценить</button>
    );
};

export default Button_estimate;