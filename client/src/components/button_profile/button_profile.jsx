import React from 'react';
import './button_profile.css'

const Button_profile = (props) => {
    return (
        <button className="button_profile"></button>
    );
};

export default Button_profile;