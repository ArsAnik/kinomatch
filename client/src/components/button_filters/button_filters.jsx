import React from 'react';
import './button_filters.css'

const Button_filters = (props) => {
    return (
        <button className="button_filters">
            <img src="src/img/icon_filters.svg" className="img_btn_filters"/>
        </button>
    );
};

export default Button_filters;